import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "www.google.com",
      "www.stamford.edu",
      "student-cms.prd.timeshighereducation.com",
      "www.amadovenigeria.net",
      "cdn.pixabay.com",
    ],
  },
  /* config options here */
};

export default nextConfig;
